import styled from 'styled-components';
import StyledComponent from './types';

export const H1: StyledComponent<HTMLHeadingElement> = styled.h1`
  margin: 0;
  padding: 12px 0;
  font-weight: bold;
  text-align: center;
`;

export const H2: StyledComponent<HTMLHeadingElement> = styled.h2`
  margin: 0;
  padding: 12px 0;
  font-weight: bold;
  text-align: center;
`;
